#include <iostream>
using namespace std;

int main( ){
    int a,b,c,sum;
    cout << "Input three numbers separated by comma: ";
    cin >> a,b,c;
    sum = a + b + c;
    cout << "The sum of the three numbers: " << sum << endl;
}